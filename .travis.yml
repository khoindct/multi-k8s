language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t khoindct132/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true khoindct132/react-test npm run test

after_success:
  - docker build -t khoindct132/multi-client ./client
  - docker build -t khoindct132/multi-nginx ./nginx
  - docker build -t khoindct132/multi-server ./server
  - docker build -t khoindct132/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push khoindct132/multi-client
  - docker push khoindct132/multi-nginx
  - docker push khoindct132/multi-server
  - docker push khoindct132/multi-worker
